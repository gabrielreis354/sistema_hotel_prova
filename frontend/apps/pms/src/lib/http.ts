/** Erro de chamada à API com o status HTTP, para a UI decidir a mensagem. */
export class ApiError extends Error {
  constructor(
    message: string,
    readonly status: number,
  ) {
    super(message);
    this.name = 'ApiError';
  }
}
